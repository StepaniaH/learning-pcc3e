
def make_shirt(size, text="I love Python!"):
    print(f"Making a {size} t-shirt with the message '{text}'")


make_shirt("large")
make_shirt("medium")

make_shirt(size="small")


"""I made a mistak to use the command print(), so besides the messages which I wanted to print, there also three `wrong` messages were printed."""
